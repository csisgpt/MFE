{
  "name": "shared",
  "root": "libs/shared",
  "sourceRoot": "libs/shared/src",
  "projectType": "library",
  "tags": ["type:lib"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint libs/shared"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config vitest.config.ts"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/shared/tsconfig.json --noEmit"
      }
    }
  }
}
