{
  "name": "shared-auth",
  "root": "libs/shared/auth",
  "sourceRoot": "libs/shared/auth/src",
  "projectType": "library",
  "tags": ["type:lib", "layer:platform"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint libs/shared/auth"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config vitest.config.ts"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/shared/tsconfig.json --noEmit"
      }
    }
  }
}
