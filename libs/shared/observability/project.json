{
  "name": "shared-observability",
  "root": "libs/shared/observability",
  "sourceRoot": "libs/shared/observability/src",
  "projectType": "library",
  "tags": ["type:lib", "layer:foundation"],
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint libs/shared/observability"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "vitest run --config vitest.config.ts"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p libs/shared/tsconfig.json --noEmit"
      }
    }
  }
}
