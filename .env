# =========================
# Global / Shared
# =========================
VITE_DEV_HOST=csis.ir

# Base URL of shell (used by remotes + shell runtime)
SHELL_ORIGIN=http://csis.ir:4990

# Default API base (your runtime.json also sets apiBaseUrl=/api)
VITE_API_BASE_URL=/api

# Optional: where shell loads runtime config from (if used in code)
VITE_RUNTIME_CONFIG_PATH=/config/runtime.json

# Optional: for clearer logs
VITE_LOG_LEVEL=info
